package com.hino.hearts.model

class CardViewModel(
    parentIndex: String,
    childIndex: String,
    cardTitle: String
) {
    var parentIndex: String = parentIndex
    var childIndex: String = childIndex
    var cardTitle: String = cardTitle
}